<h1>Listing users</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Ap1</th>
    <th>Ap2</th>
    <th>FechaNac</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.nombre %></td>
    <td><%= user.ap1 %></td>
    <td><%= user.ap2 %></td>
    <td><%= user.fechaNac %></td>
                    <!--space.reservations.build(:date => (params[:date]), :hour => (params[:hour]))-->
    <td><%= link_to 'Show', user_path(:id => user.id, :padre_id => user.padre_id) %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <!-- por aki .........  viendo la forma correcta de pasar el id de user.id para que el showContacts funcione bien...... -->
    <td><%= link_to 'PhoneBook', user_contacts_path(:user_id => user.id) %></td>
    <td><%= link_to 'AddUser', new_user_path(:id => user.id) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>

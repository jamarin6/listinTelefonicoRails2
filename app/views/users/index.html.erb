<div class="container">
  <h1 class="centra">Listing users</h1>
  <div class="row">
    <div class="col-lg-8 col-lg-offset-2">
      <table class="table table-striped col-centered">
        <tr>
          <th>Nombre</th>
          <th>Navegación</th>
        </tr>

        <% @users.each do |user| %>
         <!-- <tr>       ################### Navegacion con link_to ##########################
            <td><%#= user.nombre + " " + user.ap1 + " " + user.ap2%></td>
            <td><%#= link_to 'Show', user_path(user.id) %>   
                <%#= link_to 'Edit', edit_user_path(user) %>
                <%#= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %>
                <%#= link_to 'PhoneBook', user_contacts_path(:user_id => user.id) %>
                <%#= link_to 'AddUser', new_user_path(:id => user.id) %>
            </td>
          </tr>
                          ##################################################################-->
          <tr>
            <td><%= user.nombre + " " + user.ap1 + " " + user.ap2%></td>
            <td>  
                <%= button_to('Show', user_path(user.id), method: :get, :class => 'btn btn-primary btn-xs')  %>
                <%= button_to('Edit', edit_user_path(user), method: :get, :class => 'btn btn-warning btn-xs')  %>
                <%= button_to('Delete', user, method: :delete, :class => 'btn btn-danger btn-xs')  %>
                <%= button_to('PhoneBook', user_contacts_path(:user_id => user.id), method: :get, :class => 'btn btn-info btn-xs')  %>
                <%= button_to('AddUser', new_user_path(:id => user.id), method: :get, :class => 'btn btn-success btn-xs')  %>
            </td>
          </tr>
          <!--###################################################################-->
        <% end %>
      </table>

      <br />

      <% if @users_empty %>
        <%= link_to 'New SuperUser', new_user_path %>
      <% end %>
    </div>
    <div class="col-lg-3">
      <br>
      <br>
      <br>
      <br>
    </div>
  </div>
</div>